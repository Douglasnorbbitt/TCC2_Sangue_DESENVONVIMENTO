@model IEnumerable<TCC2_Sangue.Models.Sangue>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TipoSangue)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TipoSangue)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SangueID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SangueID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SangueID })
        </td>
    </tr>
}

</table>
